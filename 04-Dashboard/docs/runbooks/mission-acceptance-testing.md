# Runbook: Mission Acceptance Testing

## View Suite
`GET /api/mission-acceptance` — summary with total/passed/failed counts

## View Engine Cases
`GET /api/mission-acceptance/:engineId` — 10 cases for specific engine

## View Full Catalog
`GET /api/engine-catalog` — all 15 engines with contracts and acceptance counts

## Target: 150 cases seeded, all real-world verifiable, engines match Intake selector
