# Runbook: Product Shell Validation

## Check Shell State
`GET /api/product-shell` — full UX consolidation report with sections, entry points, workflow

## Check Sections
`GET /api/product-shell/sections` — all 18 sections with role and state

## Check Output Surfacing
`GET /api/final-output-report` — how many completed tasks have visible final answers

## Check Task Experience
`GET /api/task-experience` — lifecycle stage for recent tasks

## Target: 14+ shippable sections, 100% output surfacing quality, 6-step workflow clear
