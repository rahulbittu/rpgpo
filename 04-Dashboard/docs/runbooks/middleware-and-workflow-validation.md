# Runbook: Middleware and Workflow Validation

## Middleware Coverage
`GET /api/middleware-enforcement` — shows enforced/partial/missing areas

## Workflow Completion
`GET /api/workflow-completion` — shows 13 workflows with usability status

## Target: all middleware enforced, 13/13 workflows usable
