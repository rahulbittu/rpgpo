# Runbook: Ship Blocker Resolution

## Check Blockers
`GET /api/ship-blockers` — shows all blockers with status

## Resolve a Blocker
`POST /api/ship-blockers/:id/resolve` with evidence

## Target: 0 open/in_progress blockers for GO decision
