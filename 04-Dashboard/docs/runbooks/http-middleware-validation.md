# Runbook: HTTP Middleware Validation

## Run Validation
`POST /api/http-middleware-validation/run` — runs all 8 HTTP validation cases

## View Results
`GET /api/http-middleware-validation` — latest validation run with pass/fail per case

## View Redaction Behavior
`GET /api/redaction-behavior` — records showing policy vs actual redact/deny outcomes

## Target: 8/8 cases passed, all redaction behavior correct
