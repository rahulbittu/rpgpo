# Runbook: Output Contract Validation

## Check Task Deliverable
`GET /api/task-deliverable-status/:taskId` — closure state, visibility, contract compliance

## Check All Deliverables
`GET /api/deliverable-visibility-report` — how many tasks have visible deliverables

## Check Engine Contract
`GET /api/output-contracts/:engineId` — required fields, approval model, example

## Target: 100% deliverable visibility, 0 invisible completions
