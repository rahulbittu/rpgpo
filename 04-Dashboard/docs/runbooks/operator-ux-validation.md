# Runbook: Operator UX Validation

## Checking Consistency
`GET /api/ux-consistency` — UX consistency report with journey scores

## Navigation Map
`GET /api/navigation-map` — 18-tab map with panels, drilldowns, actions

## Navigation Gaps
`GET /api/navigation-gaps` — 5 remaining gaps with fixes

## Journeys
`GET /api/operator-journeys` — 7 operator journey checks

## Targeted Refresh
`GET /api/targeted-refresh` — 8 area refresh plans
