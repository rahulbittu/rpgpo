# Template Runtime Binding

## What Gets Bound
- Provider strategy → upserts provider fits
- Governance defaults → creates operator policies
- Mission defaults, project structure, docs starters (logged, not yet fully materialized)

## Binding is auditable and visible in UI.
