# Runtime Capability Activation

## What Gets Activated
prompt recipes, provider preferences, collaboration patterns, review requirements, policy defaults, docs defaults, release defaults, observability hooks, security expectations

## Activation Decisions
allow, allow_with_override, block, block_dependency_missing, block_conflict

## Conflict Detection
When two sources define conflicting capabilities, a conflict record is created.
