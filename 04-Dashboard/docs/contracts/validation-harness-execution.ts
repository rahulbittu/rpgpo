export interface ValidationHarnessExecution { execution_id: string; environment: { server_running: boolean; host: string; port: number; latency_ms: number; validation_mode: string; timestamp: string }; phases: Array<{ phase: string; status: 'passed' | 'failed' | 'skipped'; detail: string }>; proof_run: import('./live-server-proof-run').LiveServerProofRun | null; created_at: string; }
