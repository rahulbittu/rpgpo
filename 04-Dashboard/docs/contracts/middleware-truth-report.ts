export interface MiddlewareTruthReport { report_id: string; areas: Array<{ area: string; state: 'design_only' | 'evaluated_only' | 'wired' | 'executed_and_verified'; route_count: number; enforced_count: number; verified_count: number; detail: string }>; total_areas: number; executed_and_verified: number; wired: number; evaluated_only: number; design_only: number; truth_score: number; created_at: string; }
