export interface UnconditionalGoReport { report_id: string; decision: 'unconditional_go' | 'conditional_go' | 'no_go'; confidence: { level: 'unconditional' | 'conditional' | 'insufficient'; route_proven: number; total_cases: number; detail: string }; route_validation: import('./route-level-validation-run').RouteLevelValidationRun | null; middleware_coverage: { total_protected: number; total_enforced: number; coverage_percent: number }; proof_blockers: Array<{ area: string; detail: string }>; overall_score: number; created_at: string; }
