Generate the next GPO/RPGPO architecture implementation prompt.

Important:
- Return a prompt only, not a design answer.
- Output exactly one paste-ready prompt.

Current baseline:
- Parts 19-89 complete. 160+ TS modules, ~1030 API routes, ~765+ types.

Requested part:
Part 90: Task Dependency Graph Visualization + Critical Path Analysis — Add visualization data for task execution DAGs, critical path computation, and bottleneck detection so the operator can see which subtasks are blocking completion and optimize execution order.

Output format:
Return exactly one code block containing the full prompt. No intro, no explanation, no notes.
