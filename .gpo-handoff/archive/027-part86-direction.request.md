Generate the next GPO/RPGPO architecture implementation prompt.

Important:
- Return a prompt only, not a design answer.
- Output exactly one paste-ready prompt.

Current baseline:
- Parts 19-85 being implemented. 150+ TS modules, ~1000 API routes, ~710+ types, 81+ commits.
- Full enterprise GPO: structured pipeline, parallel execution, workflow orchestrator, 17+ engines, Mission Control, persistent learning, conversations, chaining, templates, scheduler, compound workflows, backup, webhooks, analytics, health checks, RBAC, API docs, performance caching.

Gap:
The UI needs a final polish pass — the dashboard works but needs a more professional look with consistent component library, better responsive design, loading skeletons, error boundaries, and a proper dark theme with refined colors. The goal is to make it look like a premium SaaS product (Linear, Vercel, Datadog quality).

Requested part:
Part 86: Premium UI Component Library + Final Visual Polish — Create a consistent component system with refined dark theme, loading states, error boundaries, responsive breakpoints, and premium micro-interactions that make GPO look world-class.

Output format:
Return exactly one code block containing the full prompt. No intro, no explanation, no notes.
