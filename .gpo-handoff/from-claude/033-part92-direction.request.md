Generate the next GPO/RPGPO architecture implementation prompt.

Important:
- Return a prompt only, not a design answer.
- Output exactly one paste-ready prompt.

Current baseline:
- Parts 19-91 complete. 165+ TS modules, ~1050 API routes, ~795+ types, 91 commits.

Requested part:
Part 92: Prompt Optimization Engine — Build a system that analyzes which prompt patterns produce the best results per engine/provider combination, and automatically suggests prompt improvements for new tasks based on historical success data.

Output format:
Return exactly one code block containing the full prompt. No intro, no explanation, no notes.
